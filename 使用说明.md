# Codex Profile Guardian 使用说明

适用版本：v1.7.0 及后续开发版

请先完整解压 ZIP，再启动程序。目录中包含：

- `CodexProfileGuardian.exe`：主程序
- `CodexProfileGuardianSecret.exe`：向 Codex 安全提供第三方 API 凭据的本地助手

主程序首次启动会把凭据助手安装到：

`%LOCALAPPDATA%\Codex Profile Guardian\bin`

Codex 的 `auth command` 会指向这个稳定目录，不再依赖 ZIP 的临时解压路径。

## 第一次使用

1. 保存正在进行的工作；程序会在切换时自动关闭并重新打开新版 ChatGPT/Codex App。
2. 双击 `CodexProfileGuardian.exe`。
3. 在“账号”中保存当前官方登录、导入 Cockpit 账号，或添加 OpenAI-compatible API。
4. 先查看“聊天保护”健康状态，再点击目标账号的“安全切换”。
5. 切换成功后，程序会按设置重新打开 Codex。

程序会在每次切换前备份 Codex 配置、SQLite 索引、会话 JSONL 和归档状态；校验失败会自动回滚。官方登录缓存和 API Key 使用当前 Windows 用户的 DPAPI 加密，不能直接复制到另一台电脑或另一个 Windows 用户使用。

v1.6.0 起，所有账号共用当前 Codex 设置。切换只改变登录凭据、API provider 和账号卡片中选择的模型；插件、MCP、项目权限、桌面选项、聊天记录与归档状态都保持切换前原样。

官号卡片和概览页会在页面可见时每 60 秒同步一次，窗口重新聚焦时立即同步。当前官网协议只显示会员等级、每周额度、重置卡次数和更新时间，不再显示已取消的 5 小时额度。额度查询在隔离目录中运行，不保存邮箱、账号 ID、Token 或原始响应。官方账号的模型 ID 可以留空；留空表示不锁定单一模型。

v1.6.2 起，SSH 同步会先确认远端没有仍在生成的回答，再在同一次停服事务中写入账号并保护聊天。无效 API 凭据不会再被同步到 SSH；重复旧会话只会进入可恢复隔离区，归档状态和正文不会因账号切换而变化。切换完成后还会再次核对远端任务路径、归档状态和索引。

如果使用 Codex 的 SSH 远程项目，可在“设置 → SSH 远程 Codex”开启同步。官方账号同步会投送最新登录与跨平台设置；第三方 API 同步需要单独开启，开启后会把 API provider 写到远端，并将 API Key 保存到远端 `~/.codex/guardian-api-profiles/`（600 权限）。

## Claude Desktop 独立管理

在顶层切换到“Claude”，可以添加、编辑、删除和启用原生 Anthropic Messages API 供应商。Guardian 的供应商列表保存在自己的数据目录，Key 使用当前 Windows 用户的 DPAPI 加密；启用供应商或恢复官方模式前会创建加密回滚包，写入失败会自动恢复。

CC Switch 不是运行依赖。如果仍保留 CC Switch，可在卸载前点击“一次性迁移”；只有该确认操作会读取当前供应商的迁移凭据，且仅接受原生 Anthropic 格式。迁移完成后，刷新状态、切换供应商和重启 Claude Desktop 都不再访问 CC Switch。重启操作只关闭 Claude Desktop 安装目录内的进程，不影响 Claude Code CLI。

默认运行数据与备份目录：

`%LOCALAPPDATA%\Codex Profile Guardian`
